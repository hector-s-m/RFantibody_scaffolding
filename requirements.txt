# RFantibody Scaffolding — Python dependencies
#
# PyTorch, DGL, and CUDA require special index URLs.
# Install them FIRST, then install the rest from this file:
#
#   pip install torch>=2.3,<2.4 torchaudio torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install dgl -f https://data.dgl.ai/wheels/torch-2.3/cu118/repo.html
#   pip install -r requirements.txt
#   pip install -e .

# Core ML (install separately with --index-url, see above)
# torch>=2.3,<2.4
# torchaudio
# torchvision
torch-utils
numpy>=1.0,<2.0

# CUDA
cuda-python==11.8

# Graph neural networks
# dgl  # install separately, see above
e3nn

# Configuration & CLI
hydra-core
click>=8.0.0

# Bioinformatics
biotite>=0.38.0

# Data & utilities
pandas
opt-einsum
pyrsistent
icecream
